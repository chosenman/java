class ExeSll {
  public static void main(String[] arg) {

      SinglyLinkedList SLL = new SinglyLinkedList();

      SLL.add(1);
      SLL.add(2);
      SLL.add(3);
      SLL.add(4);
      SLL.printValues();
      SLL.remove();
      // SLL.remove();
      
      SLL.add(5);
      SLL.printValues();
  }
}
